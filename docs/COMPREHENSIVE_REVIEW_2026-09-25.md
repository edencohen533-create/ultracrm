# בדיקת UltraCRM — 25.09.2026

## מצב לאחר התיקון

כל 19 הבעיות המתועדות בהמשך תוקנו בקוד המקומי. התיאורים ומספרי השורות בהמשך מתעדים את המצב **לפני התיקון**; סעיפי התיקון יושמו ואינם רשימת עבודה פתוחה.

- עברו 117 בדיקות אינטגרציה, כולל 31 בדיקות רגרסיה ותרחישי קצה חדשים, וכן 50 בדיקות יחידה.
- בדיקת Chromium חוזרת על הגרסה המתוקנת עברה: התחברות וטעינת 20 מסכים, ללא שגיאות JavaScript או תגובות HTTP נכשלות שנצפו. זו בדיקת טעינה, לא כיסוי לכל פעולה בממשק.
- TypeScript ובניית production עם webpack עברו. ESLint הסתיים ללא שגיאות; 27 האזהרות הקיימות אינן חלק מהממצאים שתוקנו.
- בדיקות הרגרסיה הקבועות נמצאות ב־`tests/integration/review-crm-regressions.test.ts` וב־`tests/integration/review-messaging-regressions.test.ts`. הקבצים המקבילים ב־`.qa-review` מפנים אליהן.
- הוחלו ונבדקו 9 מיגרציות במסד QA מקומי עם RLS, כולל המיגרציה החדשה `20260925120000_automation_run_claim` המוסיפה `claimed_at`. יש להחיל אותה בסביבת היעד לפני פריסת הקוד.
- ריצת אוטומציה שנתקעה מסומנת FAILED ודורשת בדיקה לפני ניסיון נוסף. תוצאת שליחה לא ודאית נשמרת UNKNOWN ואינה נשלחת אוטומטית שוב. דחייה ודאית של הספק משחררת את הגבלת התדירות ומאפשרת retry.
- נתיבי לידים, עסקאות, ייבוא, ייצוא ואיתור כפילויות אוכפים מודול CRM. אנשי קשר, משימות והערות בסיסיים משותפים גם לחיוג ולהודעות; נשמרו זמינים בכפוף להרשאות שלהם.
- ב־25.09.2026, לבקשת המשתמש, הגרסה נפרסה לייצור ב־Vercel: `dpl_5aQGQHrKHhCcip4eiHZ5yFRUJaRa`, מצב Ready. הדומיין `https://ultracrm-edencohen533-9754s-projects.vercel.app` חובר לגרסה החדשה. המיגרציה `20260925120000_automation_run_claim` הוחלה ואומתה במסד הייצור. לא בוצעו commit או push. נתוני QA ושליחות מדומות נשארו מקומיים.

## ממצאי הבדיקה המקורית

נמצאו 19 בעיות נפרדות, עם 20 תרחישי שחזור שנכשלו מול ההתנהגות המצופה. ארבע מהבעיות הופיעו בסקירה הראשונית; 15 נוספות נמצאו בבדיקה המורחבת. חיפוש לידים וחיפוש עסקאות נבדקו בנפרד, אך נספרים כבעיה אחת בעלת אותו גורם.

הבדיקה המורחבת הורצה על הקוד המקומי; בסיום הרצות השחזור HEAD היה `301b6198ed7607d4c60b22b70a14311f125f2615`, בענף `main`. הענף השתנה בזמן הבדיקה מחוץ לפעולות הסקירה. לא בוצעו בסקירה commit, merge, push או שינוי בקוד המוצר.

## שיטת הבדיקה וגבולותיה

- הוקם PostgreSQL מקומי נפרד: `127.0.0.1:55449/ultracrm_review`; כל 8 המיגרציות הוחלו בהצלחה, ו־RLS היה פעיל.
- בניית production עם webpack עברה, כולל TypeScript. בניית Turbopack נחסמה על ידי סביבת ההרצה (`Operation not permitted` בפתיחת פורט פנימי), ולכן אינה נספרת כבאג במוצר.
- בדיקת דפדפן ב־Chromium עברה: התחברות עם משתמש דמו וטעינת 20 מסכים מרכזיים, ללא שגיאות JavaScript וללא תגובות HTTP נכשלות שנצפו. זו בדיקת טעינה בסיסית, לא בדיקה של כל פעולה בכל מסך.
- 86 בדיקות האינטגרציה הקיימות עברו. בסקירה הראשונית עברו גם 50 בדיקות היחידה ו־TypeScript; ESLint החזיר 27 אזהרות וללא שגיאות.
- נוספו שתי חבילות שחזור ב־`.qa-review/`: הרשאות/CRM ושליחות/אוטומציות. 20 הבדיקות מכילות ציפיות להתנהגות התקינה ונכשלו לפני התיקון. אלה בדיקות לתקלות, לא בדיקות שמצהירות שההתנהגות השגויה תקינה.
- בדיקות השירותים משתמשות במסד אמיתי וב־RLS, עם נטרול הפעלה אסינכרונית של עיבוד אירועים ברקע כדי למנוע מרוצים לא קשורים. בדיקות API מפעילות את מטפלי הנתיבים עם JWT חתום.
- כשלי ספק שוחזרו באמצעות ספק SMS מדומה; לא נשלחו הודעות אמיתיות ולא נבדק חשבון ספק חי. תרחיש ניתוק הרשת מדגים מחיקת מידע וחזרה אפשרית לשליחה, ולא הוכחה שנשלח SMS כפול בפועל אצל ספק חיצוני.
- לא הופעלו בדיקות מול מסד הייצור. הנתונים שנוצרו הם נתוני QA ודמו במסד המקומי בלבד.

## בעיות בעדיפות גבוהה

### 1. חיפוש מבטל את הגבלת הגישה ללידים ולעסקאות — R01, R02

**קוד:** `src/lib/crm/pipeline.ts:45`, `:157`.

סינון הבעלות נבנה במפתח `OR`, וסינון החיפוש נכתב לאחריו באותו מפתח. הוספת `q` דורסת את הגבלת הבעלות. נציג שלא קיבל את הליד ברשימה הרגילה קיבל אותו בחיפוש; גם עסקה של נציג אחר חזרה בחיפוש.

**תיקון:** לצרף את סינון ההרשאות וסינון החיפוש באמצעות `AND`, ולהחיל את אותו scope גם על ספירות וסיכומים.

### 2. ציר הזמן חושף תוכן של שיחות שנחסמות בתיבת ההודעות — R03

**קוד:** `src/lib/crm/timeline.ts:24`; נתיב `src/app/api/contacts/[id]/timeline/route.ts`.

השיחה הוקצתה לנציג ב׳. `getConversationForUser` החזיר `null` לנציג א׳, אך `contactTimeline` החזיר לו את גוף ההודעה `PRIVATE MESSAGE`. סינון המשתמש חל על שיחות טלפון ומשימות, אך לא על הודעות והערות.

**תיקון:** להחיל `buildConversationScope` על הודעות והערות הקשורות לשיחות, כולל תחום הצוות/המספר.

### 3. קריאת ליד לפי מזהה עוקפת את הגבלת הרשימה — R11

**קוד:** `src/app/api/leads/[id]/route.ts:9`; אותו חוסר סינון קיים גם ב־`src/app/api/deals/[id]/route.ts:9`.

קריאת רשימת הלידים אינה חושפת ליד של נציג אחר ללא חיפוש, אבל קריאת פרטיו עם המזהה מחזירה HTTP 200. שוחזר בנתיב הלידים; הנתיב המקביל של עסקאות זוהה בקריאת הקוד.

**תיקון:** להחיל הרשאות בעלות/צוות גם בקריאה לפי מזהה, וביחסים הנכללים בתשובה.

### 4. נציג יכול להמיר ליד של נציג אחר לעסקה — R04

**קוד:** `src/lib/crm/pipeline.ts:106`; `src/app/api/leads/[id]/convert/route.ts`.

`updateLead` בודק שהליד אינו שייך לנציג אחר, אך `convertLead` אינו מבצע בדיקה מקבילה. השחזור יצר עסקה ושינה את סטטוס הליד באמצעות נציג שאינו הבעלים שלו.

**תיקון:** להשתמש בבדיקת הרשאה משותפת לפני כל שינוי בליד, כולל המרה.

### 5. יצירת הערה מאפשרת קישור לשיחה של עסק אחר — R05

**קוד:** `src/lib/crm/pipeline.ts:357`.

`createNote` מאמת רק את איש הקשר. `conversationId` ו־`dealId` נכתבים ללא בדיקת העסק ואיש הקשר שלהם. נוצרה בפועל הערה של עסק א׳ המקושרת לשיחה של עסק ב׳, למרות RLS פעיל. זהו כשל בשלמות הקשרים ובבידוד כתיבה; השחזור אינו מוכיח קריאת תוכן שיחה של עסק ב׳.

**תיקון:** לאמת כל קשר מול העסק ואיש הקשר, ולשקול אילוצי מסד שמונעים קשרים בין עסקים.

### 6. שמירת OPTED_OUT יכולה להחזיר איש קשר לדיוור — R08

**קוד:** `src/lib/crm/contacts.ts:248`; `src/lib/suppression.ts:221`.

לאיש קשר מוסר נשלח עדכון עם `consentStatus: "OPTED_OUT"`, `isBlocked: false` ואסמכתה. התוצאה במסד הייתה `OPTED_IN`, משום שביטול חסימה מלאה מתפרש כהסכמה מחודשת ומבטל את ההסרות.

**תיקון:** להפריד בין חסימה מלאה לבין הסכמה שיווקית, ולדרוש פעולה מפורשת של הסכמה מחודשת לשיווק.

### 7. שגיאת רשת כללית מוחקת הודעה שאולי כבר נשלחה — M03

**קוד:** `src/server/services/channel-send-service.ts:165`; `src/server/channels/sms/telnyx-sms.ts:60`.

ספק מדומה זרק שגיאת ניתוק לאחר כניסה לפעולת השליחה. רשומת ההודעה נמחקה, במקום להישאר `UNKNOWN`. הקמפיין מוחזר להמתנה ויכול לשלוח שוב לאחר חידוש. מתאם Telnyx אינו מעביר את `idempotencyKey` לפעולת השליחה, ולכן אין כאן הגנה מקבילה בצד הספק בקוד.

**תיקון:** להבחין בין כשל לפני שיגור לכשל שתוצאתו לא ודאית; לשמר את הרשומה ולדרוש בירור לפני שליחה נוספת.

## בעיות תפעול ותקינות נתונים

### 8. שגיאת ספק זמנית חוסמת את הניסיון החוזר — M01

**קוד:** `src/server/services/channel-send-service.ts:159`.

אחרי תשובת 429, ההודעה מסומנת `retryable`, אבל `lastMarketingAt` נשאר תפוס. ניסיון נוסף עם מפתח חדש נכשל בהגבלת תדירות של 24 שעות. ב־campaign-runner שגיאת המדיניות הזו עלולה להפוך את הנמען ל־`SKIPPED` במקום להשלים retry.

**תיקון:** לשחרר את ההזמנה להגבלת התדירות כאשר הספק דחה את ההודעה באופן ודאי, ולבדוק את מחזור ה־retry המלא.

### 9. Timeout מסומן בקמפיין כדילוג ולא כתוצאה לא ודאית — M02

**קוד:** `src/server/services/channel-send-service.ts:155`; `src/jobs/campaign-runner.ts:135`.

ההודעה נשמרה `UNKNOWN`, אבל הנמען בקמפיין נשמר `SKIPPED`, משום שה־timeout נזרק כ־`MessagePolicyError`. ניסיון חוזר מבוקר מותר רק ל־FAILED/UNKNOWN ולכן אינו נגיש לנמען הזה.

**תיקון:** להשתמש בסוג שגיאה נפרד לתוצאה לא ודאית ולשמר את הסטטוס גם בנמען.

### 10. רצף מסומן כהושלם גם כשהספק דחה את ההודעה — M04

**קוד:** `src/server/services/sequence-service.ts:216`.

ספק החזיר הודעה בסטטוס `FAILED`. העובד שמר את מזהה ההודעה והמשיך ל־`COMPLETED` בלי לבדוק את הסטטוס. היסטוריית הרצף עלולה להציג שלב שנשלח אף שנכשל.

**תיקון:** לבדוק סטטוס ותוצאת ספק לפני התקדמות לשלב הבא, עם מדיניות כשל/ניסיון חוזר מפורשת.

### 11. אוטומציה שנקטעה נשארת RUNNING ללא התאוששות — M05

**קוד:** `src/jobs/automation-runner.ts:14`, `:25`.

הוכנסה ריצה ישנה במצב `RUNNING`, המדמה עובד שנקטע אחרי תפיסת הריצה. הרצת העובד השאירה אותה במצב הזה: הוא אוסף רק `PENDING`, ואין טיפול בריצות תקועות. בניגוד לעובד הרצפים, אין זמן תפיסה ומנגנון התאוששות.

**תיקון:** להוסיף lease/זמן תפיסה וטיפול בריצה תקועה, תוך שמירה על מניעת שליחות כפולות ותוצאות לא ודאיות.

### 12. המרות מקבילות של ליד יוצרות שתי עסקאות — R13

**קוד:** `src/lib/crm/pipeline.ts:107`.

שתי קריאות מקבילות ל־`convertLead` קראו ליד לא מומר לפני העסקה במסד. שתיהן יצרו עסקה: נמצאו שתי עסקאות לאותו ליד, בעוד `lead.dealId` מחזיק רק את העדכון האחרון.

**תיקון:** לנעול ולקרוא מחדש את הליד בתוך הטרנזקציה, או להחיל מנגנון אטומי שמבטיח המרה אחת.

### 13. מיזוג אנשי קשר נכשל בהתנגשות טיוטות — R09

**קוד:** `src/lib/crm/contacts.ts:419`; `prisma/schema.prisma:1181`.

לאותו נציג היו טיוטות בשני אנשי הקשר. העברת שתיהן לאיש הקשר הראשי הפרה את האילוץ `(userId, contactId)`, והטרנזקציה כולה בוטלה עם P2002.

**תיקון:** לפתור התנגשויות טיוטה לפני העברה, תוך שמירת התוכן לפי מדיניות מוגדרת.

### 14. ייבוא איש קשר חדש אינו בודק כפילות אימייל — R12

**קוד:** `src/lib/crm/contacts.ts:369`.

נוצר איש קשר חדש עם טלפון חדש ואימייל שכבר שייך לאיש קשר אחר. המסלול לעדכון איש קשר קיים בייבוא בודק כפילות; המסלול ליצירה אינו בודק אותה. הדבר סותר את בדיקת הייחודיות הלוגית ביצירה רגילה ומקשה על זיהוי נמענים והסרות.

**תיקון:** להחיל בדיקת אימייל זהה על יצירה בייבוא ולהחזיר שגיאה בשורה המתאימה.

### 15. עדכון אימייל מתוצאת שיחה משמר hard_bounce של האימייל הקודם — R14

**קוד:** `src/lib/dialer/calls.ts:414`.

נשמר אימייל חדש דרך `saveOutcome`, אבל `emailStatus` נשאר `hard_bounce`. `deliverableEmail` ממשיך לפסול את הכתובת החדשה. במסלול עדכון איש קשר רגיל האיפוס כבר קיים, אך כאן העדכון עוקף אותו.

**תיקון:** לרכז עדכוני זהות איש קשר בשירות אחד, כולל נרמול, כפילויות ואיפוס מצב מסירה בשינוי כתובת.

### 16. חיפוש טקסט מחזיר גם רשומות שאינן תואמות — R06

**קוד:** `src/server/services/conversation-service.ts:52`; תבנית דומה ב־`src/lib/crm/pipeline.ts:46`.

חיפוש `NO_SUCH_TEXT_XYZ` החזיר שיחה ללא התאמה. הסרת כל התווים שאינם ספרות הופכת את חיפוש הטלפון ל־`contains: ""`, שמתאים לכל מספר. שוחזר בשיחות; אותו ביטוי קיים בחיפוש לידים.

**תיקון:** להוסיף את תנאי הטלפון רק כאשר קיימות מספיק ספרות בחיפוש.

### 17. כיבוי מודול CRM אינו נאכף בנתיבי CRM מרכזיים — R07

**קוד:** `src/app/api/leads/route.ts`; גם נתיבי CRM נוספים קוראים `withAuth` ללא `module`.

לאחר `modules.crm=false` ואיפוס מטמון ההרשאות, קריאת API ללידים החזירה HTTP 200. הסתרת המודול בממשק אינה מונעת שימוש ב־API.

**תיקון:** להגדיר במפורש אילו פעולות שייכות למודול וליישם בדיקה עקבית בנתיבים הרלוונטיים. פעולות CRM משותפות לחיוג/הודעות דורשות החלטה מפורשת ולא חסימה גורפת.

### 18. תשובת עדכון איש קשר מחזירה סטטוס הסכמה ישן — R10

**קוד:** `src/lib/crm/contacts.ts:256`.

לאחר עדכון מ־UNKNOWN ל־OPTED_IN, המסד הכיל OPTED_IN אך ערך ההחזרה היה UNKNOWN. `updated` נקרא לפני פעולות ההסכמה המאוחרות ומוחזר ללא קריאה מחדש.

**תיקון:** להחזיר את מצב איש הקשר הסופי אחרי כל העדכונים.

### 19. ניסיון התחלת קמפיין שנדחה צורך מכסה חודשית — M06

**קוד:** `src/server/services/campaign-service.ts:98`.

קריאת start עבור מזהה קמפיין שאינו קיים נכשלה כצפוי, אך מונה `campaigns_started` עלה מ־1 ל־2. צריכת המכסה מתרחשת לפני בדיקת קיום, preflight ומעבר סטטוס. גם ניסיונות לא תקינים יכולים למצות את המכסה.

**תיקון:** לבצע אימות תחילה ולצרוך מכסה במסגרת מעבר סטטוס אטומי, עם ביטול הזמנה במקרה כשל.

## קובצי ראיות

- `tests/integration/review-crm-regressions.test.ts`: ‏R01–R20, הרשאות, CRM וחיוג; כולן עוברות.
- `tests/integration/review-messaging-regressions.test.ts`: ‏M01–M11, שליחות ואוטומציות; כולן עוברות.
- `.qa-review/reproduction-expanded.log`: תוצאות 20 השחזורים; הכישלונות מכוונים לתעד את ההתנהגות הלא תקינה.
- `.qa-review/integration.log`: ‏117 בדיקות אינטגרציה עברו לאחר התיקון.
- `.qa-review/build-webpack.log`: בניית production עברה.
- `.qa-review/browser-smoke.json`: תוצאות טעינת 20 המסכים; `.qa-review/browser-smoke.mjs` הוא הסקריפט.
- `.qa-review/run-tests.mjs`: הרצת השחזורים עם כתובת מסד מקומית וקבועה, ללא fallback ל־`.env`. לאחר הפעלת המסד והחלת המיגרציות: `node .qa-review/run-tests.mjs`. לאחר התיקון צפוי קוד יציאה 0.
- `.qa-review/start-postgres.mjs`: מפעיל מסד מקומי ייעודי; תלוי ב־embedded-postgres שכבר הותקן במחשב, ולכן אינו סקריפט נייד ל־CI.
